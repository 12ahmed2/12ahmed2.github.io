window.addEventListener('load', function() {
    const myDiv = document.getElementById('loading_page');
    if (myDiv) { // Check if the element exists
        myDiv.style.display = 'none'; // Or 'flex', 'grid', etc., depending on desired layout
    }
});